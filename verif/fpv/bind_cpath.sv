bind control_path_add checker_cpath c1(.clk(clk), 
					.rst(rst), 
					.state_reg(state_reg), 
					.state_next(state_next), 
					.start(start),
					.rdy(rdy),
					.exp255_flag_s(exp255_flag_s),
					.op1_exp(op1_exp),
					.op2_exp(op2_exp),
					.op1_fract(op1_fract),
					.op2_fract(op2_fract),
					.ed_val(ed_val), 
					.count_s(count_s),
					.input_comb_s(input_comb_s),
					.norm_exp(norm_exp),
					.hidden_value(hidden_value),
					.n_count_s(n_count_s),
					.round_carry(round_carry),
					.round_rdy(round_rdy),
					.res_sign(res_sign)
);
